/* Какой бэкенд обслуживает сайт.
 *
 * Их бэкенд (Node + PostgreSQL) устроен иначе нашего: два сервера вместо
 * одного, другие имена полей, категории плоским списком, авторизация Basic.
 * Переключатель здесь, а не по всему коду: страницы ходят через ProductAPI
 * и AdminAPI и не знают, кто на том конце.
 *
 *   'flask' — наш бэкенд, всё как было
 *   'node'  — их бэкенд по контракту docs/api-contract.md
 *
 * У их бэкенда нет брендов, слайдера, характеристик и исполнений. Сайт от
 * этого не ломается: соответствующие разделы просто не показываются, пока
 * на той стороне не появятся эндпоинты.
 */
window.SHOP_CONFIG = {
  backend: 'node',

  // Адрес нашего API. Пусто — тот же домен, что и сайт: nginx проксирует
  // /api на Flask (см. docs/deploy-robots07.md). Заполнять только если
  // backend живёт на другом адресе, и тогда он обязан быть по https.
  apiBase: '',

  // Адреса их серверов. Публичный только на чтение, админский — на запись.
  node: {
    public: 'https://robots07.com/api',
    admin: 'http://127.0.0.1:3001',
  },
};
